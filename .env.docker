# Docker Environment Variables Configuration
PROJECT_NAME="Remote Patient Monitoring with Edge AI"
VERSION="1.0.0"

# Database Configuration (PostgreSQL container)
POSTGRES_USER=rpm_user
POSTGRES_PASSWORD=rpm_secure_password_123
POSTGRES_DB=rpm_db
DATABASE_URL=postgresql://rpm_user:rpm_secure_password_123@database:5432/rpm_db

# Security Secrets (Production Override)
SECRET_KEY=09d25e094faa6ca2556c818166b7a9563b93f7099f6f0f4caa6cf63b88e8d3e7
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
LOGIN_RATE_LIMIT_PER_MINUTE=10

# Service Inter-Container URLs
BACKEND_URL=http://backend:8000/api/edge/events
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://frontend:3000
